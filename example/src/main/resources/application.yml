spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db  # Replace with your actual database name
    username: root
    password: Sreddy@2001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # This ensures Hibernate will create/update tables automatically
    show-sql: true      # Enables SQL logging
